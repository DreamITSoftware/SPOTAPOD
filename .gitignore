# Never commit raw source data or de-identified output derived from it.
# tools/deidentify.py and tools/csharp/deidentify/ produce local-only
# output that must never end up in this repo — see tools/README.md.
*.json
!schema/*.json
!tests/fixtures/*.json

# .NET build artifacts
bin/
obj/

# OS/editor cruft
.DS_Store
*.swp
